/* eslint-disable react/prop-types */

const SectionContainer = ({ children }) => {
    return (
        <section className="w-[100%] h-screen bg-[#03001a]">{children}</section>
    );
};

export default SectionContainer;
